package mutuca.core.strategy.resource

class DefaultGatherBehavior : IGatherBehavior {

    /**
     * Our default behavior does not change gather priority, meaning that we will only collect minerals.
     */
    override fun step() {

    }
}